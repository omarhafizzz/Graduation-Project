apiVersion: v1
kind: Service
metadata:
  name: electravision
  namespace: default
spec:
  type: NodePort
  selector:
    app: electravision
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080